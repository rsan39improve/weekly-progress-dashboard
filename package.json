{
  "name": "weekly-progress-dashboard",
  "version": "1.0.0",
  "description": "週次プロジェクト進捗ダッシュボード（Google スプレッドシート → HTML → Slack）",
  "type": "module",
  "scripts": {
    "fetch": "node scripts/fetch-sheets.js",
    "dashboard": "node scripts/generate-dashboard.js",
    "deploy": "node scripts/deploy.js",
    "screenshot": "node scripts/screenshot.js",
    "slack": "node scripts/post-slack.js",
    "run-all": "node scripts/fetch-sheets.js && node scripts/generate-dashboard.js && node scripts/deploy.js && node scripts/screenshot.js && node scripts/post-slack.js"
  },
  "dependencies": {
    "@slack/web-api": "^7.3.0",
    "dotenv": "^16.4.5",
    "playwright": "^1.44.0"
  }
}
