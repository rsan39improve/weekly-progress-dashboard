# 業務週次報告ツール — 設計書

> 最終更新: 2026年5月
> ステータス: Phase 1 + AI要約機能 完成

---

## 1. ツール名

業務週次報告ツール

---

## 2. 目的

本ツールは、毎週のプロジェクト状況および担当者の作業状況を定型的に収集・整理し、チーム全体およびマネジメント者が必要な情報を迅速に把握できる状態を作ることを目的とする。

具体的な目的は以下のとおり。

1. 毎週、各プロジェクトの動向を自動的に把握すること
2. 各担当者の作業状況をチーム全体で共有し、情報の統一化を図ること
3. マネジメント者が、順調な事業・停滞している事業・危険な事業を認知し、迅速に対応できるようにすること
4. マネジメント者が各担当者の作業量を把握し、業務負荷の偏りやパンク状態を早期に検知できるようにすること

---

## 3. 基本方針

本ツールは、毎週安定して運用できることを最優先とする。

そのため、以下の方針で設計する。

1. 入力はできる限りシンプルにする（必須は3〜5分で完了）
2. 判定はまずプログラムでルールベースに行う
3. AIは自由判断ではなく、整理・要約・表現の補助として使う
4. 元データ、集計、投稿内容を分離し、運用時の事故を減らす
5. 最初から複雑な図解生成を目指さず、まずは安定稼働するMVPを作る
6. 配信先は当面Slackを優先し、LINE連携は将来拡張とする

---

## 4. 想定利用フロー

### 毎週金曜日
各担当者が、ブラウザ上の入力フォームから週次状況を入力する。

### 入力後
入力内容はGoogleスプレッドシートへ自動的に蓄積される。

### 月曜日 8:00 JST
GitHub Actions が自動起動し、以下を処理する。

- プロジェクトごとの進捗状況の集計
- 危険案件・停滞案件の抽出（ルールベース）
- 担当者ごとの負荷状況の集計
- 未提出者の確認
- AI（Gemini）による要点整理・マネジメントコメント生成
- Slackへの自動投稿

---

## 5. システム構成

### 全体フロー

```
【金曜】担当者がブラウザでフォームを入力
  ↓ GAS（doPost）でスプレッドシートに自動保存

【金曜 16:00】GAS時間トリガー → 未提出者リマインドをSlackに投稿
【金曜 18:30】GAS時間トリガー → 未提出一覧を確認・Slack通知

【月曜 8:00 JST】GitHub Actions が自動起動（PC不要・完全自動）
  ↓ run.sh を実行
  1. GAS（doGet）からスプレッドシートのデータを取得
  2. Node.js がルールベースでステータス・負荷を判定
  3. Node.js が Gemini API で要約・コメントを生成
  4. Node.js が HTML カード生成 + Playwright でスクリーンショット
  5. Slack Bot API でカード画像をチャンネルに投稿
```

### 5-1. 入力画面

- ブラウザで使えるフォーム（静的HTML）
- Tailwind CSS を用いたシンプルなUI
- 担当者が短時間で入力できる設計（必須項目は3〜5分）
- 停滞・危険を選んだ場合のみ詳細項目を追加表示
- PC・スマホのどちらでも入力可能（レスポンシブ設計）

### 5-2. データ保存先

- Googleスプレッドシート
- 全入力データを一元管理
- シート構成は後述（13章参照）

### 5-3. 集計・判定処理

- Google Apps Script（GAS）がフォームデータを受信・保存
- Node.js がスプレッドシートからデータを取得して集計・判定
- ルールベーススコアリングで案件ステータスを決定（9章参照）
- GitHub Actions 上で自動実行される

### 5-4. AI処理

- 使用モデル: Gemini（Google AI Studio）
- 呼び出し方法: Node.js から `fetch()` で Gemini API を呼び出す
- 入力: スコアリング済みの構造化JSONのみ（自由記述をそのまま渡さない）
- AIの役割: 要約・説明・表現整形に限定
- AIに任せないこと: 危険判定・未提出判定・数値集計・一次分類

### 5-5. 配信先

- Slack チャンネル（Bot API で画像投稿）
- 将来的に LINE グループ連携も検討するが、初期フェーズでは対象外

---

## 6. データの流れ

```
① 担当者がフォーム入力
② GAS（doPost）がスプレッドシート responses シートに保存
③ GitHub Actions 起動 → GAS（doGet）でデータ取得
④ Node.js がルールベースで案件ステータス・担当者負荷を判定
⑤ Node.js が Gemini API にスコアリング済みJSONを渡す
⑥ Gemini がマネジメント向けコメントを生成して返す
⑦ Node.js が HTML カードを生成し、Playwright でスクリーンショット撮影
⑧ Slack Bot API でカード画像を Slack に投稿
```

---

## 7. 役割分担（AI・プログラム・GAS）

### 7-1. GAS で行うこと

- フォーム入力データの受信・スプレッドシートへの保存（doPost）
- スプレッドシートのデータをJSON形式で返す（doGet）
- 未提出者への金曜リマインド送信（時間トリガー）

### 7-2. Node.js（GitHub Actions 上）で行うこと

- スプレッドシートからのデータ取得
- 未提出者の抽出
- 案件別の一覧整理
- ルールベーススコアリング（危険・停滞・順調の判定）
- 担当者別の負荷集計
- Gemini API の呼び出し
- HTMLカードの生成 + Playwright によるスクリーンショット撮影
- Slack への自動投稿

### 7-3. Gemini（AI）で行うこと

- 今週の重要ポイントの要約
- 危険案件の説明文作成
- 停滞案件の背景整理
- マネジメント向けコメント生成
- チーム向け総括文作成

### 7-4. AIに任せないこと

- 危険判定そのもの
- 未提出判定
- 数値集計
- 順調・停滞・危険の一次分類
- 業務負荷の定量評価

---

## 8. 入力フォーム項目

入力フォームは2段階方式とし、担当者が毎週負担なく入力できることを最優先とする。

### 8-1. 必須入力（全担当者・毎週・3〜5分）

| # | 項目 | 入力方式 | 備考 |
|---|------|---------|------|
| 1 | 担当者名 | テキスト入力 | |
| 2 | プロジェクト名 | プルダウン選択 | |
| 3 | 案件の現状 | 3択ボタン（順調・停滞・危険） | スコア算出の基点 |
| 4 | 遅延の有無 | Yes / No ボタン | |
| 5 | 支援・判断の要否 | Yes / No ボタン | |
| 6 | 今週の主な作業 | 自由記述（1〜2行） | |
| 7 | 来週の予定 | 自由記述（1〜2行） | |
| 8 | 自己負荷評価 | 1〜5のスライダー | 5が最も重い |

### 8-2. 詳細入力（「停滞」または「危険」を選んだ場合のみ表示）

| # | 項目 | 入力方式 | 備考 |
|---|------|---------|------|
| 9 | 課題・リスクの内容 | 自由記述 | |
| 10 | 相手方待ち・意思決定待ちの有無 | Yes / No ボタン | |
| 11 | 直近の重要期限 | 日付入力 | |

### 8-3. UI設計方針

- 入力項目は縦並びで分かりやすく配置
- ラジオボタン・セレクトボックスを多用し、自由記述を最小化
- 必須項目を明確にする（赤アスタリスク表示）
- 「停滞」「危険」選択時のみ詳細項目がフェードインで表示される
- スマホ表示でも崩れないレスポンシブ設計
- デザインコンセプト：手紙のように親しみやすく、実務優先

---

## 9. 判定ロジック

危険案件や停滞案件の抽出は、AIの主観ではなくルールベースのスコアで判定する。

### 9-1. 案件スコアリング

| 条件 | 加点 |
|------|------|
| 担当者が「危険」を選択 | +2点 |
| 担当者が「停滞」を選択 | +1点 |
| 遅延あり | +2点 |
| 支援・判断が必要 | +3点 |
| 相手方待ち・意思決定待ちで停止 | +2点 |
| 2週連続で進捗が乏しい | +3点 |

### 9-2. 最終ステータス判定

| スコア | ステータス |
|--------|-----------|
| 0〜2点 | 🟢 順調 |
| 3〜5点 | 🟡 停滞 |
| 6点以上 | 🔴 危険 |

### 9-3. 担当者の自己評価との関係

- 担当者の「案件の現状」選択はスコアの1要素として加算する
- 最終ステータスはプログラムのスコア判定を採用する
- 担当者の自己評価とプログラム判定が大きく乖離した場合、Slack投稿に「⚠ 要確認」フラグを付与する

### 9-4. 担当者負荷判定

以下の条件を組み合わせて、負荷偏在者を抽出する。

- 自己負荷評価 4以上
- 支援必要が ON
- 2週連続で高負荷状態

---

## 10. 出力内容

### 10-1. MVP（Phase 1）の出力

まずは週次報告の安定運用を優先し、以下のテキスト形式で出力する。

- 全体サマリー（危険〇件・停滞〇件・順調〇件）
- 危険案件一覧 + 説明文
- 停滞案件一覧
- 担当者負荷一覧
- マネジメント向けコメント（Gemini生成）

### 10-2. Phase 2以降の出力

安定稼働後に以下を追加する。

- プロジェクトごとのカード画像（Playwright生成）
- 案件状況の色分け表示（信号機カラー）
- 担当者負荷の見える化
- 前週比較
- 月次推移の可視化

---

## 11. Slack投稿イメージ

### 投稿内容の構成

1. 今週の全体サマリー
2. 危険案件一覧
3. 停滞案件一覧
4. 負荷が高い担当者一覧
5. 今週のマネジメント注目ポイント（Gemini生成）

### 投稿例（テキスト版・MVP）

```
📋 週次進捗レポート — 2026年4月7日（月）

■ サマリー
全案件: 4件
🟢 順調: 2件　🟡 停滞: 1件　🔴 危険: 1件

■ 危険案件
🔴 A事業（目黒区） — スコア: 7点
　└ 工事の見積金額が予算に合わない（遅延・支援要）

■ 停滞案件
🟡 B事業（墨田区） — スコア: 4点
　└ 相手方待ちで停止中

■ 要注意担当者
⚠ 田中さん：自己負荷5、支援要

■ 今週のポイント（AI要約）
A事業の予算超過は来週中に対応方針が必要です。
B事業は相手方への連絡状況を確認してください。
```

---

## 12. トリガー設計

| 時刻 | 処理内容 | 実行場所 |
|------|---------|---------|
| 金曜 16:00 | 未提出者リマインドをSlackに投稿 | GAS 時間トリガー |
| 金曜 18:30 | 未提出一覧を確認・Slack通知 | GAS 時間トリガー |
| 月曜 8:00 JST | 集計・AI要約・画像生成・Slack投稿 | GitHub Actions cron |

### GitHub Actions cron 設定メモ

月曜8:00（JST）= UTC で日曜23:00

```yaml
on:
  schedule:
    - cron: '0 23 * * 0'  # 日曜 23:00 UTC = 月曜 8:00 JST
```

---

## 13. データ設計（スプレッドシート構成）

### 13-1. responses（入力データ保存）

担当者の入力データをそのまま保存するシート。GASのdoPostが自動追記する。

| 列 | 項目 |
|----|------|
| A | 入力日時 |
| B | 担当者名 |
| C | プロジェクト名 |
| D | 案件の現状（順調・停滞・危険） |
| E | 遅延の有無 |
| F | 支援・判断の要否 |
| G | 今週の主な作業 |
| H | 来週の予定 |
| I | 自己負荷評価（1〜5） |
| J | 課題・リスクの内容（任意） |
| K | 相手方待ちの有無（任意） |
| L | 直近の重要期限（任意） |

### 13-2. master_projects（案件マスタ）

| 列 | 項目 |
|----|------|
| A | プロジェクトID |
| B | プロジェクト名 |
| C | 主担当者名 |
| D | 補助担当者名 |
| E | 案件区分 |
| F | 備考 |

初期登録案件:

| ID | プロジェクト名 | 主担当 |
|----|-------------|--------|
| P01 | A事業（目黒区） | |
| P02 | B事業（墨田区） | |
| P03 | C事業（埼玉県） | |
| P04 | D事業（群馬県） | |

### 13-3. weekly_summary（週次集計）

週単位で整理した案件別集計データ。Node.jsが書き込む。

| 列 | 項目 |
|----|------|
| A | 集計週（例: 2026-W15） |
| B | プロジェクト名 |
| C | 担当者名 |
| D | スコア（合計点） |
| E | 最終ステータス（順調・停滞・危険） |
| F | 担当者自己評価 |
| G | 評価乖離フラグ |
| H | 未提出フラグ |

### 13-4. workload_summary（担当者負荷集計）

担当者別の負荷集計データ。Node.jsが書き込む。

| 列 | 項目 |
|----|------|
| A | 集計週 |
| B | 担当者名 |
| C | 自己負荷評価（平均） |
| D | 支援要フラグ |
| E | 高負荷継続週数 |

### 13-5. slack_post（投稿用データ）

Slack投稿用に整形した最終出力データ。

### 13-6. settings（設定管理）

判定ルールの閾値・Slack連携設定などを管理するシート。コードを書き換えずに設定変更できる。

| 設定項目 | 値 | 備考 |
|---------|-----|------|
| 危険閾値スコア | 6 | 変更可 |
| 停滞閾値スコア | 3 | 変更可 |
| 高負荷評価基準 | 4 | 1〜5の基準値 |
| Slack チャンネルID | | |
| 集計対象週 | 自動 | |

---

## 14. 技術構成

| 役割 | 技術 | 備考 |
|------|------|------|
| 入力フォーム | HTML + Tailwind CSS + JavaScript | 静的ファイル |
| データ保存 | Google Apps Script + Google Sheets | doPost / doGet |
| 自動実行トリガー | GitHub Actions（cron） | PC不要・完全自動 |
| 集計・判定 | Node.js | スコアリングロジック |
| AI要約 | Gemini API（Google AI Studio） | Node.jsから呼び出し |
| 画像生成 | Playwright / Chromium | カードのスクリーンショット |
| Slack通知 | Slack Bot API（@slack/web-api） | 画像添付投稿 |
| 開発環境 | Cursor | |

---

## 15. ファイル構成

```
weekly-progress-dashboard/
├── DESIGN.md                    ← このファイル（設計書）
├── README.md                    ← セットアップ手順書
├── package.json
├── run.sh                       ← GitHub Actionsが実行するメインスクリプト
├── .env.example                 ← 環境変数テンプレート
├── .env                         ← 実際の設定（Git管理外）
├── .github/
│   └── workflows/
│       └── weekly-report.yml    ← GitHub Actions ワークフロー定義
├── gas/
│   └── コード.js                ← GASに貼るコード（doGet + doPost + リマインダー）
├── scripts/
│   ├── fetch-sheets.js          ← スプレッドシートからデータ取得
│   ├── score.js                 ← ルールベーススコアリング・判定
│   ├── summarize.js             ← Gemini API 呼び出し・要約生成
│   ├── generate-cards.js        ← HTMLカード生成
│   ├── screenshot.js            ← Playwright スクリーンショット撮影
│   └── post-slack.js            ← Slack Bot API で投稿
└── output/                      ← 生成ファイル（Git管理外）
    ├── mockup-form.html          ← 入力フォーム モックアップ
    ├── mockup-slack.html         ← Slackレポート イメージ確認用
    ├── mockup-card.html          ← カードデザイン確認用
    ├── data.json                 ← GASから取得したデータ
    ├── cards/                   ← プロジェクトカードHTML
    └── screenshots/             ← Slackに投稿する画像
```

---

## 16. MVP（最初に作る範囲）

初期段階では以下に機能を絞る。

1. 入力フォーム作成（2段階方式）
2. GAS で Google スプレッドシートへの保存
3. 案件別・担当者別の集計（Node.js）
4. 危険案件・負荷偏在のルールベース判定
5. GitHub Actions による月曜8時（JST）の自動実行
6. Slack へのテキスト自動投稿

この段階では、Playwright による画像生成と Gemini AI 要約は対象外とする。

---

## 17. フェーズ計画

### Phase 1 — MVP（安定稼働優先）

- 入力フォーム + GAS保存
- ルールベース判定（スコアリング）
- GitHub Actions による完全自動実行
- Slackへのテキスト投稿

### Phase 2 — ビジュアル化

- Playwright による画像カード生成と Slack 画像投稿
- Gemini API によるAI要約・マネジメントコメント
- 前週比較表示

### Phase 3 — 拡張

- 月次サマリー出力
- 管理者用の閲覧画面追加
- プロジェクトごとの履歴参照機能
- LINE連携
- 入力漏れ者への自動再通知
- 前週入力の自動引継ぎ

---

## 18. 実装ステップ

### Step 1 — 入力フォーム + シート保存

- 入力フォーム（2段階方式）の HTML を完成させる
- GAS の `doPost` を `responses` シートへの全データ保存に更新
- `master_projects` シートにA〜D事業を登録

### Step 2 — 集計・判定ロジック

- `scripts/fetch-sheets.js` でデータ取得
- `scripts/score.js` でスコアリング・ステータス判定
- 担当者負荷集計ロジックの実装

### Step 3 — GitHub Actions 設定 + Slack テキスト投稿

- `.github/workflows/weekly-report.yml` を作成
- `scripts/post-slack.js` でテキストサマリー投稿
- 月曜8時（JST）の自動実行確認

### Step 4 — AI要約（Gemini）

- `scripts/summarize.js` を作成
- Gemini API キーを GitHub Secrets に登録
- 構造化JSONを渡して要約生成・Slack投稿に組み込む

### Step 5 — 画像カード生成（Playwright）

- `scripts/generate-cards.js` でカードHTML生成
- `scripts/screenshot.js` で Playwright スクリーンショット撮影
- GitHub Actions 上での Chromium インストール設定

---

## 19. 想定リスクと対策

| リスク | 対策 |
|--------|------|
| 入力漏れ | 金曜16時・18時半のGAS自動リマインド |
| 自由記述の質のばらつき | 選択式を増やし、自由記述を補足欄に限定 |
| AIの要約ぶれ | 構造化JSONのみを渡し、要約専用プロンプトに限定 |
| 案件判定の不透明さ | ルールベーススコアで判定し、スコアを投稿に表示 |
| GitHub Actions の実行失敗 | 失敗時に管理者へSlack通知を送る仕組みを追加 |
| 運用が複雑になりすぎる | MVPで運用開始し、必要な機能のみ段階的に追加 |

---

## 20. このツールの成功条件

1. 毎週、入力が苦にならないこと（必須項目3〜5分以内）
2. 出力された情報が、管理者にとって本当に判断材料になること
3. AIの出力が毎週大きくぶれないこと
4. 誰が見ても状態が分かること（信号機カラー）
5. 運用開始後に項目変更や改善がしやすいこと

---

## 21. 結論

本ツールは、週次報告の収集そのものを目的とするのではなく、マネジメント判断に必要な情報を、毎週定型的かつ安定的に得るための仕組みとして設計する。

設計上の最重要事項：

- **入力は簡単にする** — 必須は8項目・3〜5分
- **判定はプログラムで行う** — ルールベーススコアリング
- **AIは要約と表現補助に絞る** — Gemini を構造化データのみに適用
- **完全自動で動く** — GitHub Actions が毎週月曜8時（JST）に自動実行
- **最初はテキスト投稿から** — 安定稼働を確認してから画像化

---

## 22. 実装フェーズ対応表（現状スコープ）

> このセクションは、設計書（DESIGN.md）と実装の対応関係を明示するためのものです。

### Phase 1（完了）— 基本的な自動投稿

| 機能 | 状態 | 実装場所 |
|------|------|----------|
| 入力フォーム（GitHub Pages） | ✅ 完了 | `docs/index.html` |
| GAS によるデータ保存 | ✅ 完了 | `gas/コード.js` |
| データ取得・フィルタリング（直近8日） | ✅ 完了 | `scripts/fetch-sheets.js` |
| カードHTML生成 | ✅ 完了 | `scripts/generate-dashboard.js` |
| Playwright スクリーンショット | ✅ 完了 | `scripts/screenshot.js` |
| Slack 画像投稿 | ✅ 完了 | `scripts/post-slack.js` |
| GitHub Actions 自動実行（月曜8:00 JST） | ✅ 完了 | `.github/workflows/weekly-report.yml` |

### Phase 1.5（完了）— AI要約・マネジメントサマリー

| 機能 | 状態 | 実装場所 |
|------|------|----------|
| Gemini による各PJ一言要約生成 | ✅ 完了 | `scripts/summarize.js` |
| Gemini による今週の動向生成 | ✅ 完了 | `scripts/summarize.js` |
| AIサマリーカードHTML生成 | ✅ 完了 | `scripts/generate-dashboard.js` |
| サマリーカードを1枚目に投稿 | ✅ 完了 | `scripts/post-slack.js` |
| GEMINI_API_KEY の GitHub Secrets 登録 | ✅ 完了 | GitHub Secrets |

### Phase 2（未実装）— 通知・検知機能

| 機能 | 状態 | 実装方針 |
|------|------|----------|
| 金曜リマインダー通知 | ❌ 未実装 | GASの時間トリガー または 別ワークフロー |
| 未提出者の検知 | ❌ 未実装 | マスターシートとの照合が必要 |
| ルールベーススコアリング | ❌ 未実装 | `scripts/score.js` を追加 |

### Phase 3（将来）— 分析・サマリー

| 機能 | 状態 | 実装方針 |
|------|------|----------|
| 月次サマリー | ❌ 未実装 | 月初の別ワークフローで対応 |
| 週次比較・トレンド表示 | ❌ 未実装 | スプレッドシートの複数行を参照する設計変更が必要 |
